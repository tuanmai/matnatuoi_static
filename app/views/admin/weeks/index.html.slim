h1 Danh sách tuần

table.mdl-data-table.mdl-js-data-table
  thead
    tr
      th.mdl-data-table__cell--non-numberic Tuần
      th Số Khách
      th Trạng thái
      th
      th
      th

  tbody
    - @weeks.each do |week|
      tr
        td.mdl-data-table__cell--non-numberic = link_to week.name, admin_week_products_path(week)
        td = week.customers.count
        td = week.status.humanize
        td = link_to 'Xem', admin_week_products_path(week)
        td = link_to 'Khách hàng', edit_admin_week_path(week)
        td = link_to 'Ship', admin_week_ship_info_path(week)
        td = link_to 'Xoá', admin_week_path(week), data: {:confirm => 'Chắc chưa'}, :method => :delete

br

= link_to 'Thêm Tuần mới', new_admin_week_path
